[
  {"query": "our interview feature predates the conventions and is a total mess — god-view builds the llm client inline, the store is a fake factory, dead commented files everywhere. i want to bring the whole thing into conformance. where do i even start and in what order?", "should_trigger": true},
  {"query": "i need to add a brand new 'bookmarks' feature to the app from scratch — model, indexeddb persistence, a pinia store, and the view. can you lay out the work as staged phases so i don't build the view before the store exists?", "should_trigger": true},
  {"query": "break the settings feature refactor into phases for me. it currently spans the model, store, db, and the view and i want to do it one chunk at a time", "should_trigger": true},
  {"query": "rebuild the profile feature to fit our layering rules. it's a single 500-line component doing everything right now", "should_trigger": true},
  {"query": "what order should i build the layers in when i'm rewriting a feature — model first? store first? view last? i keep starting at the view and getting stuck stubbing everything underneath", "should_trigger": true},
  {"query": "can you write me a master plan for cleaning up the insight feature so each session can pick one phase and not bleed into the others?", "should_trigger": true},
  {"query": "this whole 'onboarding' module needs modernizing — it's got logic in the view, a god store, and no db layer. plan the migration end to end", "should_trigger": true},
  {"query": "i'm executing phase 2.3 of the interview refactor plan — the store rung. what's the boundary here, what am i allowed to touch and what's out of scope until later phases?", "should_trigger": true},
  {"query": "decompose our giant ChatPage feature into staged, reviewable steps across the model, persistence, store, services and view layers", "should_trigger": true},
  {"query": "where exactly should the transform between my domain model and the indexeddb record live? right now the store does the mapping itself", "should_trigger": false},
  {"query": "review this single pinia store for me — it imports another store and does business logic in the actions, is that ok?", "should_trigger": false},
  {"query": "how should the interview screen actually look? right now it's a chat bubble transcript and i want it to read like the Lab instrument, not a generic chat app", "should_trigger": false},
  {"query": "which .nc class do i use for a live read-only coverage readout vs a normal control cell?", "should_trigger": false},
  {"query": "naming check: is getLLMClient() an ok name for a function that reads the store, builds a client, and logs on failure?", "should_trigger": false},
  {"query": "what's the bun command to add pinia to a fresh vite project and register it as a plugin in main.ts?", "should_trigger": false},
  {"query": "there's a one-line bug in the synthesis prompt builder — it's interpolating the wrong variable. can you just fix that one file?", "should_trigger": false},
  {"query": "how do i mock indexeddb in vitest? fake-indexeddb isn't resetting between test cases", "should_trigger": false},
  {"query": "should i validate internal service calls with zod too, or only the data coming back from the LLM and file imports?", "should_trigger": false},
  {"query": "convert this one Options API component to <script setup> syntax", "should_trigger": false},
  {"query": "my coverage meter overflows on mobile under 640px — fix the responsive flex css", "should_trigger": false}
]
